<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.maodd.dao.UserMapper">

    <select id="getUserList" resultType="myUser">
        select * from mybatis.user
      </select>

     <select id="getUserById" resultType="com.maodd.pojo.User" parameterType="int">
        select * from mybatis.user where id = #{id}
      </select>

    <select id="getUserByIdAndName" resultType="com.maodd.pojo.User" parameterType="map">
        select * from mybatis.user where id = #{id} and #{name}
      </select>

    <select id="getUserByNameLike" resultType="com.maodd.pojo.User" >
        select * from mybatis.user where name like #{value}
      </select>

    <insert id="asdf" >
    </insert>
<!--    增加一个用户-->
    <insert id="addUsers" parameterType="map" >
        insert into mybatis.user (id,name,pwd) value (#{tid},#{tname},#{tpwd})
    </insert>

    <insert id="addUser" parameterType="com.maodd.pojo.User" >
        insert into user (id,name,pwd) value (#{id},#{name},#{pwd})
    </insert>
    <update id="updateUser" parameterType="com.maodd.pojo.User" >
        update  user set name=#{name},pwd=#{pwd} where id = #{id}
    </update>
    <delete id="deleteUser" parameterType="int">
        delete from user where id = #{id}
    </delete>
</mapper>

